efs:
  namespace: default
  # Check the EFS region ensure it is configured for. For example, us-east-1
  #region: <EFS region>
  region: us-east-1
  #id: <ID of EFS>
  id: fs-739f91f3
  #server: <EFS Server FQDN>
  server: fs-739f91f3.efs.us-east-1.amazonaws.com
